# Container Architecture

First-class context. Entities: ContainerProvider, ContainerType, Container, ContainerRentalOrder, ContainerDelivery, ContainerSwap, ContainerPickup, ContainerEvent, ContainerCharge, ContainerSite.

Master data configurable. Fill may be manual first; IoT/GPS later without redesign.

Phase 6. No fake availability.
